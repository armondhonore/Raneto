application:
  name: raneto
  pods:
  - name: app
    image: "# filled by pipeline"
    path: /
    servicePorts:
    - 3000
    vars:
      HOST: "0.0.0.0"
      PORT: "3000"
      NODE_ENV: production
      SESSION_SECRET: "raneto_nexlayer_session_secret_key_2026"
